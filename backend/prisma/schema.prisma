generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum UserRole {
  OWNER
  ADMIN
  MANAGER
  CASHIER
  WAREHOUSE
  CHEF
  KITCHEN_HELPER
  KITCHEN_OPERATOR
}

enum TerminalDeviceType {
  POS
  KDS
  DEPOZIT
}

enum ProductClass {
  PRODUS_FIN
  MATERIE_PRIMA
  MARFA
  AMBALAJE
  AMBALAJ_SGR
  CONSUMABILE
  SEMIFABRICATE
  REZIDUALE
  ALTE_MATERIALE
  SERVICIU_VANDUT
  DISCOUNT_FINANCIAR_IESIRI
  DISCOUNT_COMERCIAL_IESIRI
  TAXA_VERDE
}

enum ProductionMode {
  AUTO
  MANUAL
}

enum WarehouseType {
  GENERAL
  RAW_MATERIALS
  FINISHED_GOODS
  BAR
  KITCHEN
  PACKAGING
}

enum StockCostMethod {
  AVG
  FIFO
  FEFO
}

enum StockMoveType {
  IN
  OUT
  TRANSFER
  ADJUST
}

enum RefType {
  SALE
  PURCHASE
  TRANSFER
  INVENTORY
  CONSUMPTION
  PRODUCTION
  MINUTES
}

enum CurrencyCode {
  RON
  EUR
  USD
  HUF
}

enum PurchaseReceiptStatus {
  DRAFT
  POSTED
  CANCELLED
}

enum TransferStatus {
  DRAFT
  POSTED
  CANCELLED
}

enum RecipeStatus {
  DRAFT
  ACTIVE
  INACTIVE
}

enum PaymentType {
  CASH
  CARD
  MIXED
}

enum DeliveryOptionSelectionMode {
  SINGLE
  MULTIPLE
}

enum DeliveryCustomerAuthProvider {
  PASSWORD
  GOOGLE
  FACEBOOK
}

enum DeliveryPaymentAttemptStatus {
  PENDING
  REDIRECTED
  PAID
  FAILED
  EXPIRED
}

enum InventoryStatus {
  DRAFT
  FINALIZED
  CANCELLED
}

enum ConsumptionDocStatus {
  DRAFT
  VALIDATED
  CANCELLED
}

enum ConsumptionDocSource {
  MANUAL
  POS_RECIPE
  SALES_AGGREGATE
}

enum SalesInvoiceStatus {
  DRAFT
  ISSUED
  CANCELLED
}

enum MinutesDocType {
  DETERIORATION
  PRICE_CHANGE
}

enum MinutesDocStatus {
  DRAFT
  POSTED
  CANCELLED
}

enum EFacturaStatus {
  NOT_READY
  PREPARED
  READY_TO_SEND
  SENT
  ACCEPTED
  REJECTED
  ERROR
}

enum IncomingEInvoiceStatus {
  SYNCED
  LINKED
}

enum MarketplacePlatform {
  GLOVO
  WOLT
  BOLT_FOOD
  GUFO_DELIVERY
}

enum IntegrationStatus {
  ACTIVE
  DISCONNECTED
  ERROR
}

enum IntegrationAuthType {
  OAUTH
  API_KEY
  PARTNER
}

enum ExternalOrderStatus {
  RECEIVED
  ACKNOWLEDGED
  IN_KITCHEN
  READY
  READY_FOR_FISCAL
  FISCALIZED
  DELIVERED
  CANCELLED
  FAILED
}

enum ExternalOrderHistorySource {
  PLATFORM
  BACKEND
  KDS
  POS
  ERP
}

enum KitchenTicketSource {
  POS_TABLE
  MARKETPLACE
}

enum KitchenTicketStatus {
  NEW
  IN_PROGRESS
  READY
  COMPLETED
  CANCELLED
}

enum SaleDraftStatus {
  OPEN
  READY_FOR_FISCAL
  FISCALIZED
  CANCELLED
}

enum SgrPackagingType {
  PET
  METAL
  STICLA
}

enum MarketplaceMappingStatus {
  MAPPED
  UNMAPPED
  PARTIAL
  BROKEN
}

model Tenant {
  id        String   @id @default(cuid())
  name      String
  subdomain String?  @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  companies Company[]
  users     User[]
  licenses  License[]
  locations Location[]
  terminals Terminal[]

  vatRates    VatRate[]
  uoms        Uom[]
  departments Department[]
  categories  Category[]
  skuCounters SkuCounter[]
  products    Product[]
  productCrossSells ProductCrossSell[]
  suppliers   Supplier[]
  customers   Customer[]
  recipes     Recipe[]

  sales                  Sale[]
  warehouses             Warehouse[]
  stockBalances          StockBalance[]
  stockLots              StockLot[]
  stockMoves             StockMove[]
  purchaseReceipts       PurchaseReceipt[]
  transferDocs           TransferDoc[]
  consumptionDocs        ConsumptionDoc[]
  productionDocs         ProductionDoc[]
  inventoryDocs          InventoryDoc[]
  minutesDocs            MinutesDoc[]
  salesInvoices          SalesInvoice[]
  incomingEInvoices      IncomingEInvoice[]
  passwordResetTokens    PasswordResetToken[]
  efacturaLogs           EFacturaLog[]
  externalIntegrations   ExternalIntegration[]
  externalOrders         ExternalOrder[]
  externalOrderHistories ExternalOrderStatusHistory[]
  kitchenTickets         KitchenTicket[]
  saleDrafts             SaleDraft[]
  marketplaceMappings    MarketplaceProductMapping[]

  subscriptions Subscription[]
  invoices      Invoice[]
  tenantModules TenantModule[]
  auditLogs     AuditLog[]
  accountingExportConfigs AccountingExportConfig[]
  accountingStockTypes    AccountingStockType[]
  backups               TenantBackup[]
  eTransportNotices    ETransportNotice[]
  anafCredentials      CompanyAnafCredential[]
}

model PlatformConfig {
  id        String   @id @default(cuid())
  key       String   @unique @default("global")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  efacturaOauthClientId     String?
  efacturaOauthClientSecret String?
  efacturaOauthRedirectUri  String?
  efacturaEnvironment       String? @default("test")
}

model Company {
    id       String  @id @default(cuid())
    tenantId String

  name    String
  code    String?
  isDefault Boolean @default(false)
  cui     String?
  regNo   String?
  address String?
  city    String?
  county  String?
  country String? @default("RO")
  postalCode String?

  bank String?
  iban String?

  email                              String?
  phone                              String?
  contactEmail                       String?
  isVatPayer                         Boolean   @default(true)
  posSyncInterval                    Int       @default(5)
  efacturaEnabled                    Boolean   @default(false)
  efacturaEnvironment                String    @default("test")
  efacturaSellerCountryCode          String    @default("RO")
  efacturaSellerCity                 String?
  efacturaSellerCounty               String?
  efacturaSellerPostalCode           String?
  efacturaContactEmail               String?
  efacturaCertSerial                 String?
  efacturaCertPasswordEnc            String?
  efacturaCertFilename               String?
  efacturaCertUploadedAt             DateTime?
  efacturaOauthClientId              String?
  efacturaOauthClientSecret          String?
  efacturaOauthRedirectUri           String?
  efacturaOauthAccessToken           String?
  efacturaOauthRefreshToken          String?
  efacturaOauthAccessTokenExpiresAt  DateTime?
  efacturaOauthRefreshTokenExpiresAt DateTime?
  efacturaOauthConnectedAt           DateTime?
  efacturaOauthLastError             String?
  etransportOauthClientId            String?
  etransportOauthClientSecret        String?
  etransportOauthRedirectUri         String?
  etransportOauthAccessToken         String?
  etransportOauthRefreshToken        String?
  etransportOauthAccessTokenExpiresAt DateTime?
  etransportOauthRefreshTokenExpiresAt DateTime?
  etransportOauthConnectedAt         DateTime?
  etransportOauthLastError           String?
  invoiceSeries                      String    @default("FAC")
  purchaseSeries                     String    @default("NIR")
  transferSeries                     String    @default("TRF")
  inventorySeries                    String    @default("INV")
  multiWarehouseEnabled              Boolean   @default(false)
  warehouseFilterEnabled             Boolean   @default(false)
  requireWarehouseOnDocuments        Boolean   @default(false)
  autoSelectSingleWarehouse          Boolean   @default(true)
  warehouseLabel                     String    @default("Gestiune")
  consumptionSeries                  String    @default("BC")
  productionSeries                   String    @default("PROD")
  deteriorationSeries                String    @default("PVD")
  priceChangeSeries                  String    @default("PVP")
  customerCodePrefix                 String    @default("CLI")
  supplierCodePrefix                 String    @default("FUR")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

    tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  products Product[]
  warehouses Warehouse[]
  suppliers Supplier[]
    customers Customer[]
  purchaseReceipts PurchaseReceipt[]
  incomingEInvoices IncomingEInvoice[]
  salesInvoices SalesInvoice[]
  recipes Recipe[]
  locations Location[]
  terminals Terminal[]
  vatRates VatRate[]
  uoms Uom[]
  departments Department[]
  categories Category[]
  sales Sale[]
    stockBalances StockBalance[]
    stockLots StockLot[]
    stockMoves StockMove[]
  consumptionDocs ConsumptionDoc[]
  productionDocs ProductionDoc[]
  transferDocs TransferDoc[]
  eTransportNotices ETransportNotice[]
  inventoryDocs InventoryDoc[]
  minutesDocs MinutesDoc[]
  userAccesses UserCompanyAccess[]
  accountingExportConfig AccountingExportConfig?
  accountingStockTypes   AccountingStockType[]
  anafCredentials CompanyAnafCredential[]

    @@index([tenantId])
    @@unique([tenantId, name])
    @@unique([tenantId, code])
  }

model CompanyAnafCredential {
  id        String   @id @default(cuid())
  tenantId  String
  companyId String

  label      String
  isDefault  Boolean @default(false)

  certSerial      String?
  certPasswordEnc String?
  certFilename    String?
  certUploadedAt  DateTime?

  efacturaOauthAccessToken           String?
  efacturaOauthRefreshToken          String?
  efacturaOauthAccessTokenExpiresAt  DateTime?
  efacturaOauthRefreshTokenExpiresAt DateTime?
  efacturaOauthConnectedAt           DateTime?
  efacturaOauthLastError             String?

  etransportOauthAccessToken           String?
  etransportOauthRefreshToken          String?
  etransportOauthAccessTokenExpiresAt  DateTime?
  etransportOauthRefreshTokenExpiresAt DateTime?
  etransportOauthConnectedAt           DateTime?
  etransportOauthLastError             String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tenant  Tenant  @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  company Company @relation(fields: [companyId], references: [id], onDelete: Cascade)

  @@index([tenantId, companyId])
  @@index([companyId, isDefault])
}

model AccountingExportConfig {
  id                    String   @id @default(cuid())
  tenantId              String
  companyId             String   @unique
  exportTarget          String   @default("SAGA")
  articleCodeSource     String   @default("SKU")
  managementAnalytic    String   @default("LOCATION_CODE")
  customerAccount       String   @default("4111")
  supplierAccount       String   @default("401")
  salesAccount          String   @default("707")
  expenseAccount        String   @default("607")
  inventoryAccount      String   @default("371")
  vatCollectedAccount   String   @default("4427")
  vatDeductibleAccount  String   @default("4426")
  cashAccount           String   @default("5311")
  cardAccount           String   @default("5121")
  defaultStockTypeId    String?
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  tenant           Tenant               @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  company          Company              @relation(fields: [companyId], references: [id], onDelete: Cascade)
  defaultStockType AccountingStockType? @relation("DefaultAccountingStockType", fields: [defaultStockTypeId], references: [id], onDelete: SetNull)

  @@index([tenantId])
  @@index([defaultStockTypeId])
}

model AccountingStockType {
  id                String   @id @default(cuid())
  tenantId          String
  companyId         String
  code              String
  name              String
  inventoryAccount  String
  expenseAccount    String
  salesAccount      String?
  analyticMode      String   @default("NONE")
  isDefault         Boolean  @default(false)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  tenant               Tenant                  @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  company              Company                 @relation(fields: [companyId], references: [id], onDelete: Cascade)
  products             Product[]
  exportConfigDefaults AccountingExportConfig[] @relation("DefaultAccountingStockType")

  @@unique([tenantId, companyId, code])
  @@index([tenantId])
  @@index([companyId])
}

model User {
  id           String   @id @default(cuid())
  tenantId     String
  email        String
  name         String
  imageUrl     String?
  passwordHash String
  mustChangePassword Boolean @default(false)
  posPinHash   String?
  role         UserRole @default(ADMIN)
  isActive     Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id])
  companyAccesses UserCompanyAccess[]
  passwordResetTokens PasswordResetToken[]
  webSessions WebSession[]

  @@unique([tenantId, email])
  @@index([tenantId])
}

model WebSession {
  id              String   @id @default(cuid())
  tenantId        String?
  userId          String?
  role            String
  email           String?
  activeCompanyId String?
  controlPanel    Boolean  @default(false)
  expiresAt       DateTime
  revokedAt       DateTime?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  user User? @relation(fields: [userId], references: [id], onDelete: SetNull)

  @@index([tenantId])
  @@index([userId])
  @@index([expiresAt])
  @@index([revokedAt])
}

model DeliveryCustomerAccount {
  id             String                       @id @default(cuid())
  email          String?                      @unique
  phone          String?                      @unique
  fullName       String
  passwordHash   String?
  authProvider   DeliveryCustomerAuthProvider @default(PASSWORD)
  providerUserId String?
  isActive       Boolean                      @default(true)
  lastLoginAt    DateTime?
  createdAt      DateTime                     @default(now())
  updatedAt      DateTime                     @updatedAt

  addresses DeliveryCustomerAddress[]
  sessions  DeliveryCustomerSession[]
  paymentMethods DeliveryCustomerPaymentMethod[]
  restaurantReviews DeliveryRestaurantReview[]
  announcementReads DeliveryAnnouncementRead[]

  @@unique([authProvider, providerUserId])
  @@index([createdAt])
}

model DeliveryCustomerSession {
  id         String   @id @default(cuid())
  customerId String
  expiresAt  DateTime
  lastSeenAt DateTime?
  revokedAt  DateTime?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  customer DeliveryCustomerAccount @relation(fields: [customerId], references: [id], onDelete: Cascade)

  @@index([customerId])
  @@index([expiresAt])
  @@index([revokedAt])
}

model DeliveryCustomerAddress {
  id          String   @id @default(cuid())
  customerId  String
  label       String
  addressLine String
  details     String?
  city        String?
  county      String?
  country     String?
  postalCode  String?
  latitude    Decimal? @db.Decimal(9, 6)
  longitude   Decimal? @db.Decimal(9, 6)
  isDefault   Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  customer DeliveryCustomerAccount @relation(fields: [customerId], references: [id], onDelete: Cascade)

  @@index([customerId])
  @@index([customerId, isDefault])
}

model DeliveryCustomerPaymentMethod {
  id                  String   @id @default(cuid())
  customerId          String
  integrationId       String
  provider            String
  token               String
  cardUniqueReference String
  label               String
  brand               String?
  maskedPan           String?
  isDefault           Boolean  @default(false)
  isActive            Boolean  @default(true)
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  customer DeliveryCustomerAccount @relation(fields: [customerId], references: [id], onDelete: Cascade)

  @@unique([customerId, provider, integrationId, cardUniqueReference])
  @@index([customerId, isDefault])
  @@index([customerId, integrationId, isActive])
}

model DeliveryCardVerificationAttempt {
  id                String                       @id @default(cuid())
  customerId        String
  integrationId     String
  status            DeliveryPaymentAttemptStatus @default(PENDING)
  vivaOrderCode     String?                      @unique
  vivaTransactionId String?
  checkoutUrl       String?
  expiresAt         DateTime?
  verifiedAt        DateTime?
  failedAt          DateTime?
  createdAt         DateTime                     @default(now())
  updatedAt         DateTime                     @updatedAt

  @@index([customerId])
  @@index([customerId, integrationId])
  @@index([status])
}

model DeliveryPaymentAttempt {
  id                  String                       @id @default(cuid())
  customerId          String?
  tenantId            String
  locationId          String
  integrationId       String
  provider            String
  methodCode          String
  status              DeliveryPaymentAttemptStatus @default(PENDING)
  vivaOrderCode       String?                      @unique
  vivaTransactionId   String?
  externalOrderId     String?
  currency            CurrencyCode                 @default(RON)
  amount              Decimal                      @default(0) @db.Decimal(12, 2)
  checkoutUrl         String?
  requestPayloadJson  Json?
  responsePayloadJson Json?
  checkoutPayloadJson Json
  expiresAt           DateTime?
  paidAt              DateTime?
  failedAt            DateTime?
  createdAt           DateTime                     @default(now())
  updatedAt           DateTime                     @updatedAt

  @@index([customerId])
  @@index([tenantId])
  @@index([locationId])
  @@index([integrationId])
  @@index([status])
  @@index([createdAt])
}

model DeliveryRestaurantReview {
  id              String   @id @default(cuid())
  customerId      String
  integrationId   String
  externalOrderId String   @unique
  rating          Int
  comment         String?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  customer      DeliveryCustomerAccount @relation(fields: [customerId], references: [id], onDelete: Cascade)
  integration   ExternalIntegration     @relation(fields: [integrationId], references: [id], onDelete: Cascade)
  externalOrder ExternalOrder           @relation(fields: [externalOrderId], references: [id], onDelete: Cascade)

  @@unique([customerId, externalOrderId])
  @@index([integrationId])
  @@index([customerId])
}

model DeliveryAnnouncement {
  id            String   @id @default(cuid())
  integrationId String?
  title         String
  body          String
  isPublished   Boolean  @default(true)
  publishedAt   DateTime @default(now())
  expiresAt     DateTime?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  integration ExternalIntegration?      @relation(fields: [integrationId], references: [id], onDelete: Cascade)
  reads       DeliveryAnnouncementRead[]

  @@index([integrationId])
  @@index([isPublished, publishedAt])
  @@index([expiresAt])
}

model DeliveryAnnouncementRead {
  id             String   @id @default(cuid())
  announcementId String
  customerId     String
  readAt         DateTime @default(now())

  announcement DeliveryAnnouncement    @relation(fields: [announcementId], references: [id], onDelete: Cascade)
  customer     DeliveryCustomerAccount @relation(fields: [customerId], references: [id], onDelete: Cascade)

  @@unique([announcementId, customerId])
  @@index([customerId])
}

model UserCompanyAccess {
  userId    String
  companyId String
  createdAt DateTime @default(now())

  user    User    @relation(fields: [userId], references: [id], onDelete: Cascade)
  company  Company @relation(fields: [companyId], references: [id], onDelete: Cascade)

  @@id([userId, companyId])
  @@index([companyId])
}

model PasswordResetToken {
  id         String   @id @default(cuid())
  tenantId   String
  userId     String
  tokenHash  String   @unique
  expiresAt  DateTime
  usedAt     DateTime?
  createdAt  DateTime @default(now())

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([tenantId])
  @@index([userId])
  @@index([expiresAt])
}

model License {
  id          String   @id @default(cuid())
  tenantId    String
  keyHash     String   @unique
  keyPrefix   String
  expiresAt   DateTime
  isSuspended Boolean  @default(false)

  modDashboard    Boolean @default(true)
  modDocuments    Boolean @default(true)
  modInventory    Boolean @default(true)
  modNomenclature Boolean @default(true)
  modSettings     Boolean @default(true)
  modPos          Boolean @default(true)
  modKds          Boolean @default(false)
  modReports      Boolean @default(false)

  limitLocations  Int @default(1)
  limitTerminals  Int @default(1)
  limitKdsDevices Int @default(1)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id])

  @@index([tenantId])
}

model Location {
  id        String   @id @default(cuid())
  tenantId  String
  companyId String?
  name      String
  code      String
  address   String?
  city      String?
  county    String?
  country   String?  @default("RO")
  postalCode String?
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tenant               Tenant                @relation(fields: [tenantId], references: [id])
  company              Company?              @relation(fields: [companyId], references: [id])
  terminals            Terminal[]
  sales                Sale[]
  warehouses           Warehouse[]
  stockBalances        StockBalance[]
  stockLots            StockLot[]
  stockMoves           StockMove[]
  purchaseReceipts     PurchaseReceipt[]
  transferDocsFrom     TransferDoc[]         @relation("TransferFromLocation")
  transferDocsTo       TransferDoc[]         @relation("TransferToLocation")
  consumptionDocs      ConsumptionDoc[]
  productionDocs       ProductionDoc[]
  inventoryDocs        InventoryDoc[]
  minutesDocs          MinutesDoc[]
  salesInvoices        SalesInvoice[]
  externalIntegrations ExternalIntegration[]
  externalOrders       ExternalOrder[]
  kitchenTickets       KitchenTicket[]
  saleDrafts           SaleDraft[]

  @@unique([tenantId, companyId, code])
  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([companyId])
}

model Warehouse {
  id         String        @id @default(cuid())
  tenantId   String
  companyId  String?
  locationId String
  code       String
  name       String
  type       WarehouseType @default(GENERAL)
  isDefault  Boolean       @default(false)
  isActive   Boolean       @default(true)
  createdAt  DateTime      @default(now())
  updatedAt  DateTime      @updatedAt

  tenant   Tenant   @relation(fields: [tenantId], references: [id])
  company  Company? @relation(fields: [companyId], references: [id])
  location Location @relation(fields: [locationId], references: [id])

  stockBalances    StockBalance[]
  stockLots        StockLot[]
  stockMoves       StockMove[]
  purchaseReceipts PurchaseReceipt[]
  consumptionDocs  ConsumptionDoc[]
  productionDocs   ProductionDoc[]
  inventoryDocs    InventoryDoc[]
  minutesDocs      MinutesDoc[]
  transferDocsFrom TransferDoc[] @relation("TransferFromWarehouse")
  transferDocsTo   TransferDoc[] @relation("TransferToWarehouse")

  @@unique([tenantId, companyId, locationId, code])
  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([locationId])
}

model Terminal {
  id                 String  @id @default(cuid())
  tenantId           String
  companyId          String?
  locationId         String?
  deviceId           String
  deviceType         TerminalDeviceType @default(POS)
  label              String?
  isLockedToLocation Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tenant   Tenant    @relation(fields: [tenantId], references: [id])
  company  Company?  @relation(fields: [companyId], references: [id])
  location Location? @relation(fields: [locationId], references: [id])
  sales    Sale[]
  departmentAccesses TerminalDepartmentAccess[]
  categoryAccesses   TerminalCategoryAccess[]
  productAccesses    TerminalProductAccess[]

  @@unique([tenantId, companyId, deviceId])
  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([companyId])
  @@index([locationId])
}

model VatRate {
  id         String   @id @default(cuid())
  tenantId   String
  companyId  String?
  name       String
  rate       Int
  fiscalCode String?
  isActive   Boolean  @default(true)
  createdAt  DateTime @default(now())

  tenant        Tenant                @relation(fields: [tenantId], references: [id])
  company       Company?              @relation(fields: [companyId], references: [id])
  products      Product[]
  receiptItems  PurchaseReceiptItem[]
  transferItems TransferDocItem[]

  @@unique([tenantId, companyId, rate])
  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([companyId])
}

model Uom {
  id        String   @id @default(cuid())
  tenantId  String
  companyId String?
  code      String
  name      String
  standardCode String?
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())

  tenant           Tenant                @relation(fields: [tenantId], references: [id])
  company          Company?              @relation(fields: [companyId], references: [id])
  stockProducts    Product[]             @relation("StockUom")
  purchaseProducts Product[]             @relation("PurchaseUom")
  receiptItems     PurchaseReceiptItem[]
  transferItems    TransferDocItem[]

  @@unique([tenantId, companyId, code])
  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([companyId])
}

model Department {
  id        String   @id @default(cuid())
  tenantId  String
  companyId String?
  name      String
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())

  tenant             Tenant              @relation(fields: [tenantId], references: [id])
  company            Company?            @relation(fields: [companyId], references: [id])
  categories         Category[]
  products           Product[]
  kitchenTicketItems KitchenTicketItem[]
  terminalAccesses   TerminalDepartmentAccess[]

  @@unique([tenantId, companyId, name])
  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([companyId])
}

model Category {
  id             String   @id @default(cuid())
  tenantId       String
  companyId      String?
  departmentId   String?
  parentCategoryId String?
  name           String
  imageUrl       String?
  posSortOrder   Int      @default(0)
  isActive       Boolean  @default(true)
  isVisibleInPos Boolean  @default(true)
  createdAt      DateTime @default(now())

  tenant     Tenant      @relation(fields: [tenantId], references: [id])
  company    Company?    @relation(fields: [companyId], references: [id])
  department Department? @relation(fields: [departmentId], references: [id])
  parentCategory Category? @relation("CategoryHierarchy", fields: [parentCategoryId], references: [id], onDelete: SetNull)
  childCategories Category[] @relation("CategoryHierarchy")
  products   Product[]
  terminalAccesses TerminalCategoryAccess[]

  @@unique([tenantId, companyId, name])
  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([companyId])
  @@index([departmentId])
  @@index([parentCategoryId])
  @@index([tenantId, companyId, posSortOrder])
}

model SkuCounter {
  id        String   @id @default(cuid())
  tenantId  String
  key       String
  value     Int      @default(0)
  updatedAt DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id])

  @@unique([tenantId, key])
  @@index([tenantId])
}

model Product {
    id       String @id @default(cuid())
    tenantId String
    companyId String?

  sku      String
  name     String
  imageUrl String?
  class    ProductClass @default(PRODUS_FIN)
  ncCode   String?
  isFiscalRiskProduct Boolean @default(false)
  netWeightKg Decimal @default(0)
  grossWeightKg Decimal @default(0)

  vatRateId      String
  uomId          String
  purchaseUomId  String?
  purchaseFactor Decimal @default(1)

  departmentId String?
  categoryId   String?
  accountingStockTypeId String?
  accountingItemCode String?

  price          Decimal        @default(0)
  deliveryDescription String?
  deliveryPromoPrice  Decimal?
  posPromoPrice       Decimal?
  costPrice      Decimal        @default(0)
  trackLot       Boolean        @default(false)
  trackExpiry    Boolean        @default(false)
  costMethod     StockCostMethod @default(AVG)
  requiresRecipe Boolean        @default(false)
  posSortOrder   Int            @default(0)
  isActive       Boolean        @default(true)
  isMenu         Boolean        @default(false)
  posMenuCategory String?
  isVisibleInPos Boolean        @default(true)
  isVisibleInDelivery Boolean   @default(true)
  publishToGlovo Boolean        @default(false)
  includeInNomenclatorExport Boolean @default(true)
  isSgr          Boolean        @default(false)
  sgrValue       Decimal        @default(0)
  sgrPackagingType SgrPackagingType?
  sgrVolumeLiters Decimal        @default(0)
  productionMode ProductionMode @default(AUTO)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

    tenant      Tenant      @relation(fields: [tenantId], references: [id])
    company     Company?    @relation(fields: [companyId], references: [id])
    vatRate     VatRate     @relation(fields: [vatRateId], references: [id])
  uom         Uom         @relation("StockUom", fields: [uomId], references: [id])
  purchaseUom Uom?        @relation("PurchaseUom", fields: [purchaseUomId], references: [id])
  department  Department? @relation(fields: [departmentId], references: [id])
  category    Category?   @relation(fields: [categoryId], references: [id])
  accountingStockType AccountingStockType? @relation(fields: [accountingStockTypeId], references: [id])

  barcodes                 ProductBarcode[]
  saleItems                SaleItem[]
  stockBalances            StockBalance[]
  stockLots                StockLot[]
  stockMoves               StockMove[]
  receiptItems             PurchaseReceiptItem[]
  transferItems            TransferDocItem[]
  eTransportNoticeItems    ETransportNoticeItem[]
  consumptionItems         ConsumptionDocItem[]  @relation("ConsumptionIngredientProduct")
  finishedConsumptionItems ConsumptionDocItem[]  @relation("ConsumptionFinishedProduct")
  productionItems          ProductionDocItem[]
  inventoryItems           InventoryDocItem[]
  salesInvoiceItems        SalesInvoiceItem[]
  minutesDocItems          MinutesDocItem[]
  externalOrderItems       ExternalOrderItem[]
  kitchenTicketItems       KitchenTicketItem[]

  recipe              Recipe?                     @relation("RecipeProduct")
  usedInRecipes       RecipeItem[]                @relation("RecipeIngredientProduct")
  marketplaceMappings MarketplaceProductMapping[]
  incomingEInvoiceItems IncomingEInvoiceItem[]
  terminalAccesses      TerminalProductAccess[]
  crossSellLinks        ProductCrossSell[]        @relation("ProductCrossSellSource")
  crossSellLinkedFrom   ProductCrossSell[]        @relation("ProductCrossSellTarget")
  deliveryOptionGroups  DeliveryProductOptionGroup[]
  deliveryOptionItems   DeliveryOptionGroupItem[]

    @@unique([tenantId, companyId, sku])
    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([tenantId, name])
    @@index([companyId])
    @@index([departmentId])
    @@index([categoryId])
    @@index([posSortOrder])
  @@index([purchaseUomId])
  @@index([accountingStockTypeId])
}

model ProductCrossSell {
  id              String   @id @default(cuid())
  tenantId        String
  sourceProductId String
  targetProductId String
  sortOrder       Int      @default(0)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  tenant        Tenant  @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  sourceProduct Product @relation("ProductCrossSellSource", fields: [sourceProductId], references: [id], onDelete: Cascade)
  targetProduct Product @relation("ProductCrossSellTarget", fields: [targetProductId], references: [id], onDelete: Cascade)

  @@unique([sourceProductId, targetProductId])
  @@index([tenantId])
  @@index([sourceProductId, sortOrder])
  @@index([targetProductId])
}

model DeliveryOptionGroup {
  id            String                      @id @default(cuid())
  tenantId      String
  companyId     String?
  name          String
  description   String?
  selectionMode DeliveryOptionSelectionMode @default(MULTIPLE)
  minSelections Int                         @default(0)
  maxSelections Int                         @default(1)
  sortOrder     Int                         @default(0)
  isActive      Boolean                     @default(true)
  createdAt     DateTime                    @default(now())
  updatedAt     DateTime                    @updatedAt

  productLinks DeliveryProductOptionGroup[]
  items        DeliveryOptionGroupItem[]

  @@index([tenantId, companyId])
  @@index([tenantId, isActive, sortOrder])
}

model DeliveryProductOptionGroup {
  id        String              @id @default(cuid())
  productId String
  groupId   String
  allowedItemIds Json?
  sortOrder Int                 @default(0)
  createdAt DateTime            @default(now())
  updatedAt DateTime            @updatedAt

  product Product             @relation(fields: [productId], references: [id], onDelete: Cascade)
  group   DeliveryOptionGroup @relation(fields: [groupId], references: [id], onDelete: Cascade)

  @@unique([productId, groupId])
  @@index([productId, sortOrder])
  @@index([groupId, sortOrder])
}

model DeliveryOptionGroupItem {
  id              String              @id @default(cuid())
  groupId         String
  productId       String
  priceAdjustment Decimal             @default(0)
  isDefault       Boolean             @default(false)
  sortOrder       Int                 @default(0)
  isActive        Boolean             @default(true)
  createdAt       DateTime            @default(now())
  updatedAt       DateTime            @updatedAt

  group   DeliveryOptionGroup @relation(fields: [groupId], references: [id], onDelete: Cascade)
  product Product             @relation(fields: [productId], references: [id], onDelete: Restrict)

  @@unique([groupId, productId])
  @@index([groupId, sortOrder])
  @@index([productId])
}

model TerminalDepartmentAccess {
  id           String   @id @default(cuid())
  terminalId   String
  departmentId String
  createdAt    DateTime @default(now())

  terminal   Terminal   @relation(fields: [terminalId], references: [id], onDelete: Cascade)
  department Department @relation(fields: [departmentId], references: [id], onDelete: Cascade)

  @@unique([terminalId, departmentId])
  @@index([terminalId])
  @@index([departmentId])
}

model TerminalCategoryAccess {
  id         String   @id @default(cuid())
  terminalId String
  categoryId String
  createdAt  DateTime @default(now())

  terminal Terminal @relation(fields: [terminalId], references: [id], onDelete: Cascade)
  category Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  @@unique([terminalId, categoryId])
  @@index([terminalId])
  @@index([categoryId])
}

model TerminalProductAccess {
  id         String   @id @default(cuid())
  terminalId String
  productId  String
  createdAt  DateTime @default(now())

  terminal Terminal @relation(fields: [terminalId], references: [id], onDelete: Cascade)
  product  Product  @relation(fields: [productId], references: [id], onDelete: Cascade)

  @@unique([terminalId, productId])
  @@index([terminalId])
  @@index([productId])
}

model Recipe {
    id        String       @id @default(cuid())
    tenantId  String
    companyId String?
    productId String       @unique
  code      String?
  name      String?
  notes     String?
  yieldQty  Decimal      @default(1)
  status    RecipeStatus @default(DRAFT)
  isActive  Boolean      @default(true)
  createdAt DateTime     @default(now())
  updatedAt DateTime     @updatedAt

    tenant  Tenant       @relation(fields: [tenantId], references: [id], onDelete: Cascade)
    company Company?     @relation(fields: [companyId], references: [id])
    product Product      @relation("RecipeProduct", fields: [productId], references: [id], onDelete: Cascade)
    items   RecipeItem[]

    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([companyId])
    @@index([productId])
    @@index([status])
  }

model RecipeItem {
  id           String   @id @default(cuid())
  recipeId     String
  ingredientId String
  qty          Decimal
  lossPercent  Decimal  @default(0)
  sortOrder    Int      @default(0)
  notes        String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  recipe     Recipe  @relation(fields: [recipeId], references: [id], onDelete: Cascade)
  ingredient Product @relation("RecipeIngredientProduct", fields: [ingredientId], references: [id])

  @@unique([recipeId, ingredientId])
  @@index([recipeId])
  @@index([ingredientId])
  @@index([sortOrder])
}

model ProductBarcode {
  id        String   @id @default(cuid())
  tenantId  String
  productId String
  barcode   String
  createdAt DateTime @default(now())

  product Product @relation(fields: [productId], references: [id])

  @@unique([tenantId, barcode])
  @@index([tenantId])
  @@index([productId])
}

model Supplier {
    id         String   @id @default(cuid())
    tenantId   String
    companyId  String?
    name       String
  code       String?
  cif        String?
  regCom     String?
  address    String?
  city       String?
  county     String?
  country    String?
  postalCode String?
  phone      String?
  email      String?
  vatPayer   Boolean?
  isActive   Boolean  @default(true)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

    tenant   Tenant            @relation(fields: [tenantId], references: [id])
    company  Company?          @relation(fields: [companyId], references: [id])
    receipts PurchaseReceipt[]
    incomingEInvoices IncomingEInvoice[]

    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([tenantId, name])
    @@index([companyId])
  }

model Customer {
    id         String   @id @default(cuid())
    tenantId   String
    companyId  String?
    name       String
  code       String?
  cif        String?
  regNo      String?
  address    String?
  city       String?
  county     String?
  country    String?
  postalCode String?
  phone      String?
  email      String?
  vatPayer   Boolean?
  isActive   Boolean  @default(true)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

    tenant   Tenant         @relation(fields: [tenantId], references: [id])
    company  Company?       @relation(fields: [companyId], references: [id])
    invoices SalesInvoice[]

    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([tenantId, name])
    @@index([companyId])
  }

model Sale {
    id              String  @id @default(cuid())
    tenantId        String
    companyId       String?
    locationId      String
  terminalId      String?
  externalOrderId String? @unique
  consumptionBatchDocId String?
  consumptionBatchProcessedAt DateTime?

  clientSaleId String?
  receiptNo    String?
  soldAt       DateTime

  total               Decimal @default(0)
  subtotal            Decimal @default(0)
  merchandiseSubtotal Decimal @default(0)
  sgrTotal            Decimal @default(0)
  discountTotal       Decimal @default(0)
  lineDiscountTotal   Decimal @default(0)
  cartDiscountTotal   Decimal @default(0)
  cartDiscountPercent Decimal @default(0)

  paymentType  PaymentType @default(CASH)
  cashAmount   Decimal?    @db.Decimal(12, 2)
  cardAmount   Decimal?    @db.Decimal(12, 2)
  operatorName String?

  createdAt DateTime @default(now())

    tenant        Tenant         @relation(fields: [tenantId], references: [id])
    company       Company?       @relation(fields: [companyId], references: [id])
    location      Location       @relation(fields: [locationId], references: [id])
  terminal      Terminal?      @relation(fields: [terminalId], references: [id])
  externalOrder ExternalOrder? @relation(fields: [externalOrderId], references: [id])
  consumptionBatchDoc ConsumptionDoc? @relation("ConsumptionBatchSales", fields: [consumptionBatchDocId], references: [id])

  items           SaleItem[]
  consumptionDocs ConsumptionDoc[]

    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([companyId])
    @@index([locationId])
  @@index([terminalId])
  @@index([soldAt])
  @@index([externalOrderId])
  @@index([consumptionBatchDocId])
  @@index([consumptionBatchProcessedAt])
}

model SaleItem {
  id        String @id @default(cuid())
  saleId    String
  productId String

  qty                     Decimal
  unitPrice               Decimal
  vatRate                 Int
  lineTotalBeforeDiscount Decimal @default(0)
  discountPercent         Decimal @default(0)
  lineDiscountTotal       Decimal @default(0)
  lineTotalAfterDiscount  Decimal @default(0)

  sale    Sale    @relation(fields: [saleId], references: [id])
  product Product @relation(fields: [productId], references: [id])

  @@index([saleId])
  @@index([productId])
}

model PosDailyClosure {
  id            String   @id @default(cuid())
  tenantId      String
  companyId     String?
  locationId    String?
  terminalId    String?
  deviceId      String?
  locationName  String?
  terminalLabel String?
  reportType    String   @default("Z")
  reportNo      String?
  closedAt      DateTime @default(now())
  total         Decimal  @default(0) @db.Decimal(12, 2)
  cashTotal     Decimal  @default(0) @db.Decimal(12, 2)
  cardTotal     Decimal  @default(0) @db.Decimal(12, 2)
  otherTotal    Decimal  @default(0) @db.Decimal(12, 2)
  reportText    String?
  payloadJson   Json?
  createdAt     DateTime @default(now())

  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([companyId])
  @@index([locationId])
  @@index([terminalId])
  @@index([closedAt])
}

model StockBalance {
    id         String @id @default(cuid())
    tenantId   String
    companyId  String?
    locationId String
    warehouseId String?
    warehouseScope String @default("__NO_WAREHOUSE__")
    productId  String

  qty Decimal @default(0)

    tenant   Tenant   @relation(fields: [tenantId], references: [id])
    company  Company? @relation(fields: [companyId], references: [id])
    location Location @relation(fields: [locationId], references: [id])
    warehouse Warehouse? @relation(fields: [warehouseId], references: [id])
    product  Product  @relation(fields: [productId], references: [id])

    @@unique([tenantId, companyId, locationId, productId, warehouseScope])
    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([companyId])
    @@index([locationId])
    @@index([warehouseId])
    @@index([productId])
  }

model StockLot {
    id                   String   @id @default(cuid())
    tenantId             String
    companyId            String?
    locationId           String
    warehouseId          String?
    productId            String
    sourceReceiptId      String?
    sourceReceiptItemId  String?
    lotNo                String
    expiryDate           DateTime?
    receivedAt           DateTime @default(now())
    initialQty           Decimal  @default(0)
    remainingQty         Decimal  @default(0)
    unitCostNetRon       Decimal  @default(0)
    totalRemainingValue  Decimal  @default(0)
    createdAt            DateTime @default(now())
    updatedAt            DateTime @updatedAt

    tenant            Tenant               @relation(fields: [tenantId], references: [id])
    company           Company?             @relation(fields: [companyId], references: [id])
    location          Location             @relation(fields: [locationId], references: [id])
    warehouse         Warehouse?           @relation(fields: [warehouseId], references: [id])
    product           Product              @relation(fields: [productId], references: [id])
    sourceReceipt     PurchaseReceipt?     @relation(fields: [sourceReceiptId], references: [id])
    sourceReceiptItem PurchaseReceiptItem? @relation(fields: [sourceReceiptItemId], references: [id])
    stockMoves        StockMove[]
    consumptionAllocations ConsumptionDocItemLot[] @relation("ConsumptionDocItemLotStockLot")
    transferSourceAllocations TransferDocItemLot[] @relation("TransferDocItemLotSourceStockLot")
    transferDestinationAllocations TransferDocItemLot[] @relation("TransferDocItemLotDestinationStockLot")

    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([companyId])
    @@index([locationId])
    @@index([warehouseId])
    @@index([productId])
    @@index([sourceReceiptId])
    @@index([sourceReceiptItemId])
    @@index([productId, receivedAt])
    @@index([productId, expiryDate])
  }

model StockMove {
    id         String @id @default(cuid())
    tenantId   String
    companyId  String?
    locationId String
    warehouseId String?
    productId  String
    lotId      String?

  type StockMoveType
  qty  Decimal
  unitCost   Decimal @default(0)
  totalValue Decimal @default(0)

  refType RefType?
  refId   String?
  refItemId String?
  note    String?

  createdAt DateTime @default(now())

    tenant   Tenant   @relation(fields: [tenantId], references: [id])
    company  Company? @relation(fields: [companyId], references: [id])
    location Location @relation(fields: [locationId], references: [id])
    warehouse Warehouse? @relation(fields: [warehouseId], references: [id])
    product  Product  @relation(fields: [productId], references: [id])
    lot      StockLot? @relation(fields: [lotId], references: [id])

    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([companyId])
    @@index([locationId])
  @@index([warehouseId])
  @@index([productId])
  @@index([lotId])
  @@index([refType, refId])
  @@index([refItemId])
}

model ConsumptionDoc {
  id         String   @id @default(cuid())
  tenantId   String
  companyId  String?
  locationId String
  warehouseId String?
  saleId     String?
  aggregateParentId String?
  docNo      String
  docDate    DateTime
  source     ConsumptionDocSource @default(MANUAL)
  sourcePeriodStart DateTime?
  sourcePeriodEnd DateTime?
  status     ConsumptionDocStatus @default(DRAFT)
  note       String?
  totalValue Decimal  @default(0)
  validatedAt DateTime?
  validatedBy String?
  cancelledAt DateTime?
  cancelledBy String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

    tenant   Tenant   @relation(fields: [tenantId], references: [id])
    company  Company? @relation(fields: [companyId], references: [id])
    location Location @relation(fields: [locationId], references: [id])
    warehouse Warehouse? @relation(fields: [warehouseId], references: [id])
    sale     Sale?    @relation(fields: [saleId], references: [id])
    batchSales Sale[] @relation("ConsumptionBatchSales")
    aggregateParent ConsumptionDoc? @relation("ConsumptionDocAggregation", fields: [aggregateParentId], references: [id])
    sourceDocs ConsumptionDoc[] @relation("ConsumptionDocAggregation")

  items ConsumptionDocItem[]

    @@unique([tenantId, companyId, docNo])
    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([companyId])
  @@index([locationId])
  @@index([warehouseId])
  @@index([saleId])
  @@index([aggregateParentId])
  @@index([docDate])
}

model ConsumptionDocItem {
  id                String   @id @default(cuid())
  consumptionDocId  String
  finishedProductId String?
  ingredientId      String
  qty               Decimal
  unitCost          Decimal  @default(0)
  totalCost         Decimal  @default(0)
  costMethod        String?
  note              String?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  consumptionDoc  ConsumptionDoc @relation(fields: [consumptionDocId], references: [id], onDelete: Cascade)
  finishedProduct Product?       @relation("ConsumptionFinishedProduct", fields: [finishedProductId], references: [id])
  ingredient      Product        @relation("ConsumptionIngredientProduct", fields: [ingredientId], references: [id])
  lotAllocations  ConsumptionDocItemLot[]

  @@index([consumptionDocId])
  @@index([finishedProductId])
  @@index([ingredientId])
}

model ConsumptionDocItemLot {
  id                   String   @id @default(cuid())
  consumptionDocItemId String
  stockLotId           String
  qty                  Decimal
  unitCost             Decimal  @default(0)
  totalCost            Decimal  @default(0)
  createdAt            DateTime @default(now())

  consumptionDocItem ConsumptionDocItem @relation(fields: [consumptionDocItemId], references: [id], onDelete: Cascade)
  stockLot           StockLot           @relation("ConsumptionDocItemLotStockLot", fields: [stockLotId], references: [id])

  @@index([consumptionDocItemId])
  @@index([stockLotId])
}

model ProductionDoc {
    id         String   @id @default(cuid())
    tenantId   String
    companyId  String?
    locationId String
  warehouseId String?
  docNo      String
  docDate    DateTime
  note       String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

    tenant   Tenant   @relation(fields: [tenantId], references: [id])
    company  Company? @relation(fields: [companyId], references: [id])
    location Location @relation(fields: [locationId], references: [id])
    warehouse Warehouse? @relation(fields: [warehouseId], references: [id])

  items ProductionDocItem[]

    @@unique([tenantId, companyId, docNo])
    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([companyId])
    @@index([locationId])
  @@index([warehouseId])
  @@index([docDate])
}

model ProductionDocItem {
  id              String   @id @default(cuid())
  productionDocId String
  productId       String
  qty             Decimal
  createdAt       DateTime @default(now())

  productionDoc ProductionDoc @relation(fields: [productionDocId], references: [id], onDelete: Cascade)
  product       Product       @relation(fields: [productId], references: [id])

  @@index([productionDocId])
  @@index([productId])
}

model PurchaseReceipt {
    id           String       @id @default(cuid())
    tenantId     String
    companyId    String?
    locationId   String
  warehouseId  String?
  supplierId   String?
  supplierName String?
  supplierCode String?
  docNo        String
  docDate      DateTime
  currency     CurrencyCode @default(RON)
  fxRate       Decimal      @default(1)

  totalNetFc   Decimal @default(0)
  totalVatFc   Decimal @default(0)
  totalGrossFc Decimal @default(0)

  totalNetRon   Decimal @default(0)
  totalVatRon   Decimal @default(0)
  totalGrossRon Decimal @default(0)

  note   String?
  status PurchaseReceiptStatus @default(DRAFT)
  sourceIncomingEInvoiceId String? @unique
  spvDownloadId String?
  spvUploadIndex String?
  spvInvoiceNo String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

    tenant   Tenant                @relation(fields: [tenantId], references: [id])
    company  Company?              @relation(fields: [companyId], references: [id])
    location Location              @relation(fields: [locationId], references: [id])
  warehouse Warehouse?            @relation(fields: [warehouseId], references: [id])
  supplier Supplier?             @relation(fields: [supplierId], references: [id])
  sourceIncomingEInvoice IncomingEInvoice? @relation(fields: [sourceIncomingEInvoiceId], references: [id])
  items    PurchaseReceiptItem[]
  stockLots StockLot[]

    @@unique([tenantId, companyId, docNo])
    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([companyId])
    @@index([locationId])
  @@index([warehouseId])
    @@index([supplierId])
  @@index([status])
  @@index([docDate])
  @@index([spvDownloadId])
}

model PurchaseReceiptItem {
  id        String @id @default(cuid())
  receiptId String
  productId String
  uomId     String
  lotNo     String?
  expiryDate DateTime?

  qty              Decimal @default(0)
  conversionFactor Decimal @default(1)
  stockQty         Decimal @default(0)

  unitCostNetFc  Decimal @default(0)
  unitCostNetRon Decimal @default(0)

  lineNetFc   Decimal @default(0)
  lineVatFc   Decimal @default(0)
  lineGrossFc Decimal @default(0)

  lineNetRon   Decimal @default(0)
  lineVatRon   Decimal @default(0)
  lineGrossRon Decimal @default(0)

  vatRateId    String?
  vatRateValue Decimal @default(19)

  createdAt DateTime @default(now())

  receipt PurchaseReceipt @relation(fields: [receiptId], references: [id], onDelete: Cascade)
  product Product         @relation(fields: [productId], references: [id])
  uom     Uom             @relation(fields: [uomId], references: [id])
  vatRate VatRate?        @relation(fields: [vatRateId], references: [id])
  stockLots StockLot[]

  @@index([receiptId])
  @@index([productId])
  @@index([uomId])
  @@index([vatRateId])
  @@index([lotNo])
  @@index([expiryDate])
}

model IncomingEInvoice {
    id                   String                 @id @default(cuid())
    tenantId             String
    companyId            String?
    supplierId           String?
  linkedReceiptId      String?                @unique
  spvDownloadId        String
  spvUploadIndex       String?
  spvMessageId         String?
  spvCommunicationDate DateTime?
  invoiceNo            String?
  invoiceDate          DateTime?
  currency             CurrencyCode           @default(RON)
  totalNet             Decimal                @default(0)
  totalVat             Decimal                @default(0)
  totalGross           Decimal                @default(0)
  supplierName         String?
  supplierCode         String?
  supplierCif          String?
  customerName         String?
  customerCif          String?
  status               IncomingEInvoiceStatus @default(SYNCED)
  xmlText              String
  rawPayload           Json?
  syncedAt             DateTime               @default(now())
  createdAt            DateTime               @default(now())
  updatedAt            DateTime               @updatedAt

    tenant        Tenant            @relation(fields: [tenantId], references: [id], onDelete: Cascade)
    company       Company?          @relation(fields: [companyId], references: [id])
    supplier      Supplier?         @relation(fields: [supplierId], references: [id])
  linkedReceipt PurchaseReceipt?
  items         IncomingEInvoiceItem[]

    @@unique([tenantId, companyId, spvDownloadId])
    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([companyId])
    @@index([supplierId])
  @@index([invoiceDate])
  @@index([invoiceNo])
  @@index([supplierCif])
}

model IncomingEInvoiceItem {
  id               String   @id @default(cuid())
  invoiceId        String
  matchedProductId String?
  lineIndex        Int
  productName      String
  productCode      String?
  externalCode     String?
  barcode          String?
  uomCode          String?
  qty              Decimal  @default(0)
  unitPrice        Decimal  @default(0)
  vatRate          Decimal  @default(0)
  lineNet          Decimal  @default(0)
  lineVat          Decimal  @default(0)
  lineGross        Decimal  @default(0)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  invoice        IncomingEInvoice @relation(fields: [invoiceId], references: [id], onDelete: Cascade)
  matchedProduct Product?         @relation(fields: [matchedProductId], references: [id])

  @@index([invoiceId])
  @@index([matchedProductId])
}

model TransferDoc {
    id             String         @id @default(cuid())
    tenantId       String
    companyId      String?
    fromLocationId String
  fromWarehouseId String?
  toLocationId   String
  toWarehouseId   String?
  docNo          String
  docDate        DateTime
  note           String?
  reason         String?
  delegateName   String?
  delegateCi     String?
  vehicle        String?
  vehicleNo      String?
  trailerNo      String?
  senderName     String?
  receiverName   String?
  approvedBy     String?
  eTransportOperationType String?
  eTransportPartnerCountry String?
  eTransportPartnerCui String?
  eTransportPartnerName String?
  eTransportInternalRef String?
  eTransportStartScope String?
  eTransportEndScope String?
  eTransportStartAddress String?
  eTransportEndAddress String?
  eTransportStartBorderPoint String?
  eTransportEndBorderPoint String?
  eTransportTransportDocType String?
  eTransportTransportDocNo String?
  eTransportTransportDocDate DateTime?
  eTransportTransportDocNotes String?
  eTransportExtraInfo String?
  eTransportCandidate Boolean      @default(false)
  eTransportRequired  Boolean      @default(false)
  eTransportDeclaredStart DateTime?
  eTransportVehicleMaxMassKg Decimal?
  eTransportOrganizer String?
  eTransportOperator  String?
  eTransportUit       String?
  eTransportStatus    String?
  eTransportUploadIndex String?
  eTransportDownloadId  String?
  eTransportPreparedXml String?
  eTransportErrorText   String?
  totalQty       Decimal        @default(0)
  totalValue     Decimal        @default(0)
  status         TransferStatus @default(DRAFT)
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt

    tenant       Tenant            @relation(fields: [tenantId], references: [id])
    company      Company?          @relation(fields: [companyId], references: [id])
    fromLocation Location          @relation("TransferFromLocation", fields: [fromLocationId], references: [id])
  fromWarehouse Warehouse?       @relation("TransferFromWarehouse", fields: [fromWarehouseId], references: [id])
  toLocation   Location          @relation("TransferToLocation", fields: [toLocationId], references: [id])
  toWarehouse Warehouse?         @relation("TransferToWarehouse", fields: [toWarehouseId], references: [id])
  items        TransferDocItem[]

    @@unique([tenantId, companyId, docNo])
    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([companyId])
    @@index([fromLocationId])
  @@index([fromWarehouseId])
  @@index([toLocationId])
  @@index([toWarehouseId])
  @@index([status])
  @@index([docDate])
}

model ETransportNotice {
  id                    String   @id @default(cuid())
  tenantId              String
  companyId             String?
  noticeNo              String
  sourceType            String?
  sourceId              String?
  sourceDocNo           String?
  operationType         String?
  partnerCountry        String?
  partnerCui            String?
  partnerName           String?
  partnerAddress        String?
  internalRef           String?
  startScope            String?
  endScope              String?
  startAddress          String?
  endAddress            String?
  startBorderPoint      String?
  endBorderPoint        String?
  transportDocType      String?
  transportDocNo        String?
  transportDocDate      DateTime?
  transportDocNotes     String?
  extraInfo             String?
  candidate             Boolean  @default(false)
  required              Boolean  @default(false)
  declaredStart         DateTime?
  vehicleNo             String?
  trailerNo             String?
  vehicleMaxMassKg      Decimal?
  organizerCountry      String?
  organizerCode         String?
  organizerName         String?
  organizerAddress      String?
  operatorName          String?
  uit                   String?
  status                String?
  uploadIndex           String?
  downloadId            String?
  preparedXml           String?
  errorText             String?
  totalGrossWeightKg    Decimal  @default(0)
  totalValueRon         Decimal  @default(0)
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  tenant                Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  company               Company? @relation(fields: [companyId], references: [id], onDelete: SetNull)
  items                 ETransportNoticeItem[]

  @@unique([tenantId, companyId, noticeNo])
  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([companyId])
  @@index([sourceType, sourceId])
  @@index([status])
  @@index([declaredStart])
}

model ETransportNoticeItem {
  id                    String   @id @default(cuid())
  noticeId              String
  productId             String?
  sourceItemId          String?
  lineNo                Int      @default(1)
  sku                   String?
  name                  String
  ncCode                String?
  fiscalRisk            Boolean  @default(false)
  uomCode               String?
  qty                   Decimal  @default(0)
  unitPrice             Decimal  @default(0)
  lineValue             Decimal  @default(0)
  netWeightPerUnitKg    Decimal  @default(0)
  netWeightTotalKg      Decimal  @default(0)
  grossWeightPerUnitKg  Decimal  @default(0)
  grossWeightTotalKg    Decimal  @default(0)
  internalReference     String?
  createdAt             DateTime @default(now())

  notice                ETransportNotice @relation(fields: [noticeId], references: [id], onDelete: Cascade)
  product               Product?         @relation(fields: [productId], references: [id], onDelete: SetNull)

  @@index([noticeId])
  @@index([productId])
  @@index([sourceItemId])
}

model TransferDocItem {
  id           String   @id @default(cuid())
  transferId   String
  productId    String
  uomId        String
  qty          Decimal  @default(0)
  unitPrice    Decimal  @default(0)
  lineValue    Decimal  @default(0)
  vatRateId    String?
  vatRateValue Decimal  @default(0)
  createdAt    DateTime @default(now())

  transfer TransferDoc @relation(fields: [transferId], references: [id], onDelete: Cascade)
  product  Product     @relation(fields: [productId], references: [id])
  uom      Uom         @relation(fields: [uomId], references: [id])
  vatRate  VatRate?    @relation(fields: [vatRateId], references: [id])
  lotAllocations TransferDocItemLot[]

  @@index([transferId])
  @@index([productId])
  @@index([uomId])
  @@index([vatRateId])
}

model TransferDocItemLot {
  id                    String   @id @default(cuid())
  transferDocItemId     String
  sourceStockLotId      String
  destinationStockLotId String?
  qty                   Decimal
  unitCost              Decimal  @default(0)
  totalValue            Decimal  @default(0)
  lotNo                 String
  expiryDate            DateTime?
  createdAt             DateTime @default(now())

  transferDocItem     TransferDocItem @relation(fields: [transferDocItemId], references: [id], onDelete: Cascade)
  sourceStockLot      StockLot        @relation("TransferDocItemLotSourceStockLot", fields: [sourceStockLotId], references: [id])
  destinationStockLot StockLot?       @relation("TransferDocItemLotDestinationStockLot", fields: [destinationStockLotId], references: [id])

  @@index([transferDocItemId])
  @@index([sourceStockLotId])
  @@index([destinationStockLotId])
}

model InventoryDoc {
    id          String          @id @default(cuid())
    tenantId    String
    companyId   String?
    locationId  String
  warehouseId  String?
  docNo       String
  docDate     DateTime
  note        String?
  status      InventoryStatus @default(DRAFT)
  finalizedAt DateTime?
  createdAt   DateTime        @default(now())
  updatedAt   DateTime        @updatedAt

    tenant   Tenant   @relation(fields: [tenantId], references: [id])
    company  Company? @relation(fields: [companyId], references: [id])
    location Location @relation(fields: [locationId], references: [id])
  warehouse Warehouse? @relation(fields: [warehouseId], references: [id])

  items InventoryDocItem[]

    @@unique([tenantId, companyId, docNo])
    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([companyId])
    @@index([locationId])
  @@index([warehouseId])
  @@index([docDate])
  @@index([status])
}

model InventoryDocItem {
  id             String   @id @default(cuid())
  inventoryDocId String
  productId      String
  systemQty      Decimal  @default(0)
  countedQty     Decimal  @default(0)
  differenceQty  Decimal  @default(0)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  inventoryDoc InventoryDoc @relation(fields: [inventoryDocId], references: [id], onDelete: Cascade)
  product      Product      @relation(fields: [productId], references: [id])

  @@index([inventoryDocId])
  @@index([productId])
}

model MinutesDoc {
    id         String           @id @default(cuid())
    tenantId   String
    companyId  String?
    locationId String
  warehouseId String?
  docNo      String
  docDate    DateTime
  type       MinutesDocType
  status     MinutesDocStatus @default(DRAFT)
  reasonCode String?
  findingCode String?
  note       String?
  causeNote  String?
  conclusion String?
  proposal   String?
  committeeChair   String?
  committeeMember1 String?
  committeeMember2 String?
  custodianName    String?
  responsibleName  String?
  approvedBy       String?
  totalQty   Decimal          @default(0)
  totalValue Decimal          @default(0)
  createdAt  DateTime         @default(now())
  updatedAt  DateTime         @updatedAt

    tenant   Tenant           @relation(fields: [tenantId], references: [id])
    company  Company?         @relation(fields: [companyId], references: [id])
    location Location         @relation(fields: [locationId], references: [id])
  warehouse Warehouse?       @relation(fields: [warehouseId], references: [id])
  items    MinutesDocItem[]

    @@unique([tenantId, companyId, docNo])
    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([companyId])
    @@index([locationId])
  @@index([warehouseId])
  @@index([docDate])
  @@index([type])
  @@index([status])
}

model MinutesDocItem {
  id           String   @id @default(cuid())
  minutesDocId String
  productId    String
  qty          Decimal  @default(0)
  unitValue    Decimal  @default(0)
  oldPrice     Decimal?
  newPrice     Decimal?
  lineValue    Decimal  @default(0)
  note         String?
  createdAt    DateTime @default(now())

  minutesDoc MinutesDoc @relation(fields: [minutesDocId], references: [id], onDelete: Cascade)
  product    Product    @relation(fields: [productId], references: [id])

  @@index([minutesDocId])
  @@index([productId])
}

model SalesInvoice {
    id                  String             @id @default(cuid())
    tenantId            String
    companyId           String?
    locationId          String
  customerId          String?
  docNo               String
  docDate             DateTime
  dueDate             DateTime?
  customerName        String
  customerCode        String?
  customerCif         String?
  customerRegNo       String?
  customerAddress     String?
  customerEmail       String?
  customerPhone       String?
  currency            CurrencyCode       @default(RON)
  fxRate              Decimal            @default(1)
  totalNetFc          Decimal            @default(0)
  totalDiscountFc     Decimal            @default(0)
  totalVatFc          Decimal            @default(0)
  totalGrossFc        Decimal            @default(0)
  totalSgrFc          Decimal            @default(0)
  totalWithSgrFc      Decimal            @default(0)
  totalNetRon         Decimal            @default(0)
  totalDiscountRon    Decimal            @default(0)
  totalVatRon         Decimal            @default(0)
  totalGrossRon       Decimal            @default(0)
  totalSgrRon         Decimal            @default(0)
  totalWithSgrRon     Decimal            @default(0)
  invoiceTypeCode     String             @default("380")
  isEfacturaRequired  Boolean            @default(true)
  efacturaStatus      EFacturaStatus     @default(NOT_READY)
  efacturaUploadIndex String?
  efacturaDownloadId  String?
  efacturaPreparedAt  DateTime?
  efacturaSentAt      DateTime?
  efacturaValidatedAt DateTime?
  efacturaLastCheckAt DateTime?
  efacturaDownloadedAt DateTime?
  efacturaXmlText     String?
  efacturaErrorText   String?
  note                String?
  status              SalesInvoiceStatus @default(DRAFT)
  createdAt           DateTime           @default(now())
  updatedAt           DateTime           @updatedAt

    tenant       Tenant             @relation(fields: [tenantId], references: [id])
    company      Company?           @relation(fields: [companyId], references: [id])
    location     Location           @relation(fields: [locationId], references: [id])
  customer     Customer?          @relation(fields: [customerId], references: [id])
  items        SalesInvoiceItem[]
  efacturaLogs EFacturaLog[]

    @@unique([tenantId, companyId, docNo])
    @@index([tenantId])
    @@index([tenantId, companyId])
    @@index([companyId])
    @@index([locationId])
  @@index([customerId])
  @@index([docDate])
  @@index([status])
}

model SalesInvoiceItem {
  id              String   @id @default(cuid())
  invoiceId       String
  productId       String
  productName     String   @default("")
  productCode     String?
  uomCode         String?
  uomStandardCode String?
  vatCategoryCode String?
  qty             Decimal  @default(0)
  unitPriceFc     Decimal  @default(0)
  vatRateValue    Decimal  @default(19)
  discountPercent Decimal  @default(0)
  discountAmountFc Decimal @default(0)
  lineNetFc       Decimal  @default(0)
  lineVatFc       Decimal  @default(0)
  lineGrossFc     Decimal  @default(0)
  sgrUnitFc       Decimal  @default(0)
  sgrTotalFc      Decimal  @default(0)
  discountAmountRon Decimal @default(0)
  lineNetRon      Decimal  @default(0)
  lineVatRon      Decimal  @default(0)
  lineGrossRon    Decimal  @default(0)
  sgrTotalRon     Decimal  @default(0)
  createdAt       DateTime @default(now())

  invoice SalesInvoice @relation(fields: [invoiceId], references: [id], onDelete: Cascade)
  product Product      @relation(fields: [productId], references: [id])

  @@index([invoiceId])
  @@index([productId])
}

model EFacturaLog {
  id        String   @id @default(cuid())
  tenantId  String
  invoiceId String
  stage     String
  success   Boolean  @default(false)
  message   String?
  payload   Json?
  createdAt DateTime @default(now())

  tenant  Tenant       @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  invoice SalesInvoice @relation(fields: [invoiceId], references: [id], onDelete: Cascade)

  @@index([tenantId])
  @@index([invoiceId])
  @@index([createdAt])
}

model ExternalIntegration {
  id             String              @id @default(cuid())
  tenantId       String
  locationId     String?
  platform       MarketplacePlatform
  status         IntegrationStatus   @default(DISCONNECTED)
  authType       IntegrationAuthType @default(PARTNER)
  accessToken    String?
  refreshToken   String?
  tokenExpiresAt DateTime?
  merchantId     String?
  storeId        String?
  webhookSecret  String?
  settingsJson   Json?
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt

  tenant   Tenant    @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  location Location? @relation(fields: [locationId], references: [id], onDelete: SetNull)

  orders              ExternalOrder[]
  marketplaceMappings MarketplaceProductMapping[]
  deliveryReviews     DeliveryRestaurantReview[]
  deliveryAnnouncements DeliveryAnnouncement[]

  @@index([tenantId])
  @@index([locationId])
  @@index([platform])
  @@index([status])
}

model ExternalOrder {
  id                  String              @id @default(cuid())
  tenantId            String
  locationId          String
  integrationId       String?
  platform            MarketplacePlatform
  externalOrderId     String
  externalOrderNumber String?
  status              ExternalOrderStatus @default(RECEIVED)
  customerName        String?
  customerPhone       String?
  customerNote        String?
  paymentLabel        String?
  currency            CurrencyCode        @default(RON)
  subtotal            Decimal             @default(0) @db.Decimal(12, 2)
  total               Decimal             @default(0) @db.Decimal(12, 2)
  rawPayloadJson      Json?
  placedAt            DateTime?
  acknowledgedAt      DateTime?
  readyAt             DateTime?
  fiscalizedAt        DateTime?
  cancelledAt         DateTime?
  createdAt           DateTime            @default(now())
  updatedAt           DateTime            @updatedAt

  tenant      Tenant               @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  location    Location             @relation(fields: [locationId], references: [id], onDelete: Cascade)
  integration ExternalIntegration? @relation(fields: [integrationId], references: [id], onDelete: SetNull)

  items         ExternalOrderItem[]
  statusHistory ExternalOrderStatusHistory[]
  kitchenTicket KitchenTicket?
  saleDraft     SaleDraft?
  sale          Sale?
  deliveryReviews DeliveryRestaurantReview[]

  @@unique([tenantId, platform, externalOrderId])
  @@index([tenantId])
  @@index([locationId])
  @@index([integrationId])
  @@index([status])
  @@index([createdAt])
}

model ExternalOrderItem {
  id                String                   @id @default(cuid())
  externalOrderId   String
  externalLineId    String?
  externalProductId String?
  name              String
  sku               String?
  qty               Decimal                  @default(0) @db.Decimal(12, 3)
  unitPrice         Decimal                  @default(0) @db.Decimal(12, 2)
  vatRate           Int?
  modifiersJson     Json?
  note              String?
  erpProductId      String?
  mappingStatus     MarketplaceMappingStatus @default(UNMAPPED)
  createdAt         DateTime                 @default(now())
  updatedAt         DateTime                 @updatedAt

  externalOrder ExternalOrder @relation(fields: [externalOrderId], references: [id], onDelete: Cascade)
  erpProduct    Product?      @relation(fields: [erpProductId], references: [id], onDelete: SetNull)

  @@index([externalOrderId])
  @@index([externalProductId])
  @@index([erpProductId])
  @@index([mappingStatus])
}

model ExternalOrderStatusHistory {
  id              String                     @id @default(cuid())
  tenantId        String
  externalOrderId String
  status          ExternalOrderStatus
  source          ExternalOrderHistorySource
  message         String?
  payloadJson     Json?
  createdAt       DateTime                   @default(now())

  tenant        Tenant        @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  externalOrder ExternalOrder @relation(fields: [externalOrderId], references: [id], onDelete: Cascade)

  @@index([tenantId])
  @@index([externalOrderId])
  @@index([status])
  @@index([source])
  @@index([createdAt])
}

model KitchenTicket {
  id              String              @id @default(cuid())
  tenantId        String
  locationId      String
  externalOrderId String?             @unique
  source          KitchenTicketSource
  status          KitchenTicketStatus @default(NEW)
  displayNumber   String?
  station         String?
  note            String?
  sentToKdsAt     DateTime?
  readyAt         DateTime?
  completedAt     DateTime?
  createdAt       DateTime            @default(now())
  updatedAt       DateTime            @updatedAt

  tenant        Tenant              @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  location      Location            @relation(fields: [locationId], references: [id], onDelete: Cascade)
  externalOrder ExternalOrder?      @relation(fields: [externalOrderId], references: [id], onDelete: SetNull)
  items         KitchenTicketItem[]

  @@index([tenantId])
  @@index([locationId])
  @@index([status])
  @@index([source])
}

model KitchenTicketItem {
  id              String   @id @default(cuid())
  kitchenTicketId String
  productId       String?
  name            String
  qty             Decimal  @default(0) @db.Decimal(12, 3)
  departmentId    String?
  station         String?
  note            String?
  modifiersJson   Json?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  kitchenTicket KitchenTicket @relation(fields: [kitchenTicketId], references: [id], onDelete: Cascade)
  product       Product?      @relation(fields: [productId], references: [id], onDelete: SetNull)
  department    Department?   @relation(fields: [departmentId], references: [id], onDelete: SetNull)

  @@index([kitchenTicketId])
  @@index([productId])
  @@index([departmentId])
}

model SaleDraft {
  id              String          @id @default(cuid())
  tenantId        String
  locationId      String
  externalOrderId String?         @unique
  status          SaleDraftStatus @default(OPEN)
  cartJson        Json
  subtotal        Decimal         @default(0) @db.Decimal(12, 2)
  total           Decimal         @default(0) @db.Decimal(12, 2)
  createdAt       DateTime        @default(now())
  updatedAt       DateTime        @updatedAt

  tenant        Tenant         @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  location      Location       @relation(fields: [locationId], references: [id], onDelete: Cascade)
  externalOrder ExternalOrder? @relation(fields: [externalOrderId], references: [id], onDelete: SetNull)

  @@index([tenantId])
  @@index([locationId])
  @@index([status])
}

model MarketplaceProductMapping {
  id                String                   @id @default(cuid())
  tenantId          String
  integrationId     String
  externalProductId String
  externalName      String?
  erpProductId      String?
  status            MarketplaceMappingStatus @default(UNMAPPED)
  lastSeenAt        DateTime?
  createdAt         DateTime                 @default(now())
  updatedAt         DateTime                 @updatedAt

  tenant      Tenant              @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  integration ExternalIntegration @relation(fields: [integrationId], references: [id], onDelete: Cascade)
  erpProduct  Product?            @relation(fields: [erpProductId], references: [id], onDelete: SetNull)

  @@unique([integrationId, externalProductId])
  @@index([tenantId])
  @@index([integrationId])
  @@index([erpProductId])
  @@index([status])
}

enum BillingStatus {
  OK
  FAILED
  UNPAID
}

enum BillingCycle {
  MONTHLY
  YEARLY
}

enum SubscriptionStatus {
  ACTIVE
  CANCELLED
  PAST_DUE
  TRIAL
  EXPIRED
}

enum InvoiceStatus {
  DRAFT
  ISSUED
  PAID
  UNPAID
  OVERDUE
  CANCELLED
}

enum ModuleTarget {
  GESTIUNE
  POS
  BOTH
}

enum AuditActorType {
  SYSTEM
  OWNER
  USER
}

model Plan {
  id           String       @id @default(cuid())
  code         String       @unique
  name         String
  description  String?
  priceMonthly Decimal      @default(0) @db.Decimal(12, 2)
  priceYearly  Decimal      @default(0) @db.Decimal(12, 2)
  currency     CurrencyCode @default(RON)
  isActive     Boolean      @default(true)
  createdAt    DateTime     @default(now())
  updatedAt    DateTime     @updatedAt

  subscriptions Subscription[]

  @@index([isActive])
}

model AppModule {
  id          String       @id @default(cuid())
  code        String       @unique
  name        String
  description String?
  target      ModuleTarget @default(BOTH)
  isCore      Boolean      @default(false)
  isActive    Boolean      @default(true)
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt

  tenantModules TenantModule[]

  @@index([isActive])
}

model TenantModule {
  id         String   @id @default(cuid())
  tenantId   String
  moduleId   String
  enabled    Boolean  @default(true)
  limitValue Int?
  source     String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  tenant Tenant    @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  module AppModule @relation(fields: [moduleId], references: [id], onDelete: Cascade)

  @@unique([tenantId, moduleId])
  @@index([tenantId])
  @@index([moduleId])
  @@index([enabled])
}

model Subscription {
  id                 String             @id @default(cuid())
  tenantId           String
  planId             String
  vivaSubscriptionId String?
  vivaOrderCode      String?
  billingCycle       BillingCycle
  status             SubscriptionStatus @default(TRIAL)
  billingStatus      BillingStatus      @default(OK)
  price              Decimal            @default(0) @db.Decimal(12, 2)
  currency           CurrencyCode       @default(RON)
  startDate          DateTime           @default(now())
  endDate            DateTime?
  nextBillingDate    DateTime?
  trialEndsAt        DateTime?
  graceEndsAt        DateTime?
  cancelledAt        DateTime?
  notes              String?
  createdAt          DateTime           @default(now())
  updatedAt          DateTime           @updatedAt

  tenant   Tenant    @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  plan     Plan      @relation(fields: [planId], references: [id])
  invoices Invoice[]

  @@index([tenantId])
  @@index([planId])
  @@index([status])
  @@index([billingStatus])
  @@index([vivaSubscriptionId])
}

model Invoice {
  id             String        @id @default(cuid())
  tenantId       String
  subscriptionId String?
  number         String        @unique
  issueDate      DateTime
  dueDate        DateTime
  paidAt         DateTime?
  amount         Decimal       @default(0) @db.Decimal(12, 2)
  currency       CurrencyCode  @default(RON)
  status         InvoiceStatus @default(DRAFT)
  externalRef    String?
  downloadUrl    String?
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt

  tenant       Tenant        @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  subscription Subscription? @relation(fields: [subscriptionId], references: [id], onDelete: SetNull)

  @@index([tenantId])
  @@index([subscriptionId])
  @@index([status])
  @@index([issueDate])
  @@index([dueDate])
}

model AuditLog {
  id         String         @id @default(cuid())
  tenantId   String?
  actorType  AuditActorType @default(SYSTEM)
  actorId    String?
  action     String
  entityType String
  entityId   String?
  payload    Json?
  ipAddress  String?
  userAgent  String?
  createdAt  DateTime       @default(now())

  tenant Tenant? @relation(fields: [tenantId], references: [id], onDelete: SetNull)

  @@index([tenantId])
  @@index([actorType])
  @@index([action])
  @@index([entityType])
  @@index([createdAt])
}

model TenantBackup {
  id              String   @id @default(cuid())
  tenantId        String
  companyId       String?
  createdByUserId String?
  label           String?
  fileName        String
  filePath        String
  fileSizeBytes   Int      @default(0)
  tableCounts     Json?
  createdAt       DateTime @default(now())

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId, createdAt])
  @@index([companyId])
}
